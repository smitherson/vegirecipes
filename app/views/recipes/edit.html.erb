<h1>Editing recipe</h1>

<%= render 'form' %>

<%= link_to 'Show', @recipe %> |
<%= link_to 'Back', recipes_path %>

<%=raw @products_for_recipe.map { |product| "\"#{product.product_id}\"" }.join(', ') %>

